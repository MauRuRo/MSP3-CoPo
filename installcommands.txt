pip3 install Flask
pip3 install pymongo
pip3 install dnspython
pip3 install flask_pymongo
python3 app.py

issue with search bar RETURN key
issue with Collaborator field showing when none are present.

connecting to mongo hiding the root: cd .. (home directory), nano ~/.bashrc, "export MONGO_URI = ***link***"